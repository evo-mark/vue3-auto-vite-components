-   **Feature**: Added 'resolveIndex' prop to options to allow `index.vue` files to be named after their parent directory

-   **Internal**: New tests for `resolveIndex` prop
-   **Internal**: Refactored build and release for CI/CD
